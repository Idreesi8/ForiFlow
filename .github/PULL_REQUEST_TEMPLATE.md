## Summary

What this PR changes, in one short paragraph. Link the issue if there is one.

## Type of change

- [ ] Feature
- [ ] Bug fix
- [ ] Documentation
- [ ] Refactor / chore
- [ ] Model / training

## How I verified it

- [ ] `pytest` in `backend/` (note any skipped ML tests)
- [ ] Manual path through the dashboard (which screens)
- [ ] Docker rebuild / `docker compose up` (if deployment changed)

## Risk

Does this change a score, a SHAP value, an alert threshold, or an HTTP contract?
If yes, say how a reviewer should confirm the numbers still match the host.

## Screenshots

Required for UI changes. Drop files under `docs/screenshots/` or attach here.
