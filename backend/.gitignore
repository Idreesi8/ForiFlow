__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Local SQLite databases
*.db
*.db-journal
*.sqlite3

# The serving artefacts (ml/*.pkl, ml/feature_names.json) are committed so a
# fresh clone scores with the trained ensemble. The training log is not.
ml/train_log.txt

# Raw training data (large, and licensed by its original publishers)
ml/data/*.csv
