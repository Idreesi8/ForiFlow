# ForiFlow backend dependencies (Python 3.12+; xgboost 3.4 requires it)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.30
pydantic>=2.9.0
python-dotenv>=1.0.1
alembic>=1.13.0
psycopg2-binary>=2.9.9
PyJWT>=2.9.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.x reads bcrypt.__about__, which bcrypt 4.1+ removed.
bcrypt>=4.0.1,<4.1.0

# Model serving: required to score with the trained ensemble. Without these the
# API falls back to the linear surrogate in services/scoring_service.py.
# scikit-learn and xgboost are pinned to the exact versions the shipped
# artefacts were pickled with (backend/ml/*.pkl records sklearn 1.8.0 and
# xgboost 3.4.0). Unpickling with any other version prints "might lead to
# breaking code or invalid results". Retrain before bumping either pin.
numpy>=2.0,<3
scikit-learn==1.8.0
# xgboost-cpu is the same 3.4.0 library without the ~350 MB nvidia-nccl
# dependency that the plain Linux wheel pulls in; ForiFlow never trains or
# scores on a GPU. It has no macOS wheel, so macOS keeps the regular package.
xgboost-cpu==3.4.0; sys_platform == "linux" or sys_platform == "win32"
xgboost==3.4.0; sys_platform == "darwin"
shap>=0.46.0,<0.53
joblib>=1.4.0

# Model training only (python -m ml.train_real_model)
pandas>=2.2.0
imbalanced-learn>=0.12.0
optuna>=4.0.0

# Testing
pytest>=8.3.0
httpx>=0.27.0
